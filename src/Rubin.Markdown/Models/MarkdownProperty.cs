namespace Rubin.Markdown.Models
{
    // Represents a Value of of a markdown "property"
    // [//]: # "categories: ugga (Value), bugga (Value), johnny (Value)"
    public class MarkdownProperty
    {
        public string Value { get; set; } = string.Empty;
    }
}
